# 2026-07-19 Devlog

## td-0bfdf8 — True multiuser: chose second deployment, provisioned giftlist-cv

### Summary
Caleb (+ Debbie) get their own instance of the app instead of in-app multi-tenancy. A full households/tenancy design was planned first (Option A: `households` table, ~110+ query sites to scope, 8–11.5 days) but a duplicate deployment (Option B: ~1 day, isolation by construction) won on effort/benefit — at most one more family will ever join. Both plans recorded in `docs/2026-07-19-multiuser-households-plan.md`.

### What Was Done
- **`scripts/seed-users.ts`**: optional third seed spec `SUPPORT_*` (role admin, skipped when env unset). Kept last in `SPECS` deliberately: `getAdminUserId()` picks the first admin by id for background-job/webhook attribution, which must be the household's own admin (caleb), not the support account.
- **`ecosystem-cv.config.cjs`**: PM2 app `giftlist-cv`, cwd `/opt/giftlist-cv`, port 3005 (3000–3004 taken by the other droplet apps).
- **`scripts/deploy-to-DO.sh`**: parameterized for both instances via a case-based lookup (macOS bash 3.2 — no associative arrays). Default deploys BOTH so they never drift; `--target giftlist|giftlist-cv` picks one. Public-URL health check downgraded from fatal to warning (local healthz still gates) so a first deploy can precede DNS.
- **Droplet provisioning** (`/opt/giftlist-cv`): fresh clone, own `.env` (new `AUTH_SECRET`, `PORT=3005`, `BASE_URL=https://giftlist-cv.gaylon.photos`, shared `GOOGLE_CLIENT_ID/SECRET` with cv redirect URI, shared `SHIPPO_API_KEY` with its own `SHIPPO_WEBHOOK_SECRET`, `ENABLE_CRON=true`; intentionally NO SMTP/Telegram/Anthropic keys). Built, migrated to v28, seeded caleb/debbie/gaylon, PM2 started + saved, Nginx server block added and reloaded. Local healthz OK; vhost verified via `curl -H "Host: giftlist-cv.gaylon.photos" http://134.199.211.199/healthz`.

### Key Technical Insights
- The Shippo webhook handler already answers `{ok, note:'Unknown tracking; ignored'}` for unknown tracking numbers, so both instances can be registered as webhook endpoints on the one Shippo account and each ignores the other's events.
- Reminders/Amazon scans no-op harmlessly on the cv instance: no notify channels configured and no Gmail labels — nothing to disable despite `ENABLE_CRON=true`.
- Contacts import needs zero code change: it keys the Google token off `locals.user.id`, so Caleb connecting his own Gmail at `/admin/settings` just works.

### Remaining Manual Steps (Gaylon)
1. Cloudflare DNS: A record `giftlist-cv` → 134.199.211.199, proxied (same as siblings).
2. GCP console: add `https://giftlist-cv.gaylon.photos/admin/settings/google/callback` to the OAuth client's authorized redirect URIs.
3. Shippo dashboard: add webhook `https://giftlist-cv.gaylon.photos/api/tracking/shippo?token=<SHIPPO_WEBHOOK_SECRET from /opt/giftlist-cv/.env>`.
4. Retrieve/share seeded passwords (in `/opt/giftlist-cv/.env`): `ssh root@134.199.211.199 "grep PASSWORD /opt/giftlist-cv/.env"`.

### Update (later same day)
All four manual steps completed by Gaylon (via Codex walkthrough). Verified live: public healthz OK through Cloudflare, `/login` 200, Shippo webhook route 401s without token. td-0bfdf8 closed.

### Files Modified
- `scripts/seed-users.ts`, `scripts/deploy-to-DO.sh`, `ecosystem-cv.config.cjs` (new), `docs/2026-07-19-multiuser-households-plan.md` (new)
