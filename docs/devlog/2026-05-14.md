# 2026-05-14 Devlog

## 09:30 EDT — Phase 1 remediation: three surgical fixes shipped together

### Summary
Closed three td issues in one batch — all small, well-scoped P1/P2 fixes.
Phase 2 (multi-recipient order split) is queued as a separate, larger
schema-migration body of work.

### What Changed

**td-68e82a (P1) — Birthday off-by-one in Next Up.**
`nextOccurrenceDate()` in `src/lib/server/occasions.ts` constructed
`new Date(year, month-1, day)` — a *local-timezone* midnight. Production
runs PM2 on a DigitalOcean droplet that's typically UTC. The resulting
"Dec 7 00:00 UTC" date serialized over the wire arrived in the US Eastern
browser as "Sunday, December 6". The day-of-week shift was the smoking
gun: it pointed at TZ slip, not date arithmetic. Math.round vs Math.floor
was a red herring.

Fix: anchor occurrence dates at **noon UTC**. Noon UTC is the same
calendar date in every timezone from UTC−11 through UTC+11, so JSON
round-trips preserve the intended day-of-month. Exported a small helper
`todayMidnightUTC()` and converged `today.ts`, `people.ts`, and
`reminders.ts` on it. New unit tests in `occasions.test.ts` lock in the
behavior under multiple US timezones.

**td-0c8de5 (P1) — Reminder "needs attention" conflicts with bought gifts.**
The digest's `HANDLED_STATUSES` constant in
`src/lib/server/jobs/reminders.ts` was `{given, returned}` — too narrow.
That meant Ambree's `$50 gift card` (status: `ordered`) was being routed
into the *needsAttention* bucket and rendered as "No gift is marked
bought yet" — while the very same digest's "Packages on the way" section
listed the same gift. Broadened to all post-`idea` statuses
(`planned, ordered, shipped, delivered, wrapped, given, returned`).
"Coming up" still surfaces every committed occasion through delivery,
unchanged. Updated the explanatory comment to spell out the digest's
semantic split.

**td-8de343 (P2) — Gift status badge in Coming Up Soon.**
Added `bestGiftStatus: GiftStatus | null` to `UpcomingOccasion` and a
single CASE-ordered query (`annotateGiftState`) that fetches the most-
progressed non-archived gift per `(person, occasion)`. Rendered as a
text-labeled pill in `/app/today` with three accessible tone variants
reusing existing design tokens — no new colors, AAA contrast preserved.
Existing `hasHandledGift` semantics ({given, returned}) intact so
Today's "stay until given" filter is unchanged.

### Verification
- `npm run check` — 0 errors, 0 warnings.
- `npm test` — 58/58 passing (was 52; added 6 new TZ tests).
- `npm run build` — clean.
- Manual smoke pending after deploy: Jeff Copper's detail should show
  "Turns 80 ... Monday, December 7"; running reminders job manually from
  `/admin/system` should NOT list Ambree/Bruce/Carter in needs-attention
  but should still show their packages on the way; `/app/today` rows for
  people with committed gifts should show appropriate pills.

### Files
- `src/lib/server/occasions.ts` — TZ-stable Date construction, exported
  `todayMidnightUTC()`.
- `src/lib/server/today.ts` — `UpcomingOccasion.bestGiftStatus`,
  `annotateGiftState()` replaces `markHandled()`.
- `src/lib/server/people.ts` — uses `todayMidnightUTC()`.
- `src/lib/server/jobs/reminders.ts` — broadened HANDLED_STATUSES;
  `todayMidnightUTC()`.
- `src/routes/app/today/+page.svelte` — `statusTone()` helper, pill
  render, new tone classes.
- `src/lib/server/occasions.test.ts` — new TZ test file.

### Next
Phase 2 — multi-recipient order split (td-3e9ae2 + td-6c189e):
- New `migrations/018-orders-table.sql` with backfill from existing
  `gifts.order_id` values.
- Multi-item Amazon parser; `import_rows.parsed_items_json`.
- Order-grouped review UI with per-row recipient picker + "apply same
  to all" shortcut.
- Per-line-item weak match; order-keyed Shippo registration.
- Re-import backfill for collapsed order #113-2234245-9301002.

---

## 11:00 EDT — Phase 2 remediation: multi-recipient order split

### Summary
Closed td-3e9ae2 and its design-notes companion td-6c189e in one
implementation. Schema migrated to v18 with a new `orders` table
sitting 1:N over `gifts`. Amazon imports now parse every line item
from a multi-item email, the review UI renders a per-line recipient
picker, and the commit path creates one order + N gifts. The single-
item path is unchanged in behavior — it just additionally writes a
parent orders row going forward.

### What Changed

**Schema — migrations/018-orders-table.sql.**
- New `orders` table owns order-level facts: order_id (unique),
  vendor/shipper FKs, total cents, tracking number/carrier/provider
  id/Amazon URL, lifecycle timestamps, source message id.
- `gifts` gains `order_pk` (FK) + `line_item_index` (0..N-1 within an
  order). Existing tracking columns on `gifts` stay denormalized for
  one release for back-compat with all reader code.
- `import_rows` gains `parsed_items_json` (JSON array of
  `{title, priceCents, quantity}`). Null for legacy rows.
- Backfill: for every distinct `gifts.order_id`, INSERT one synthetic
  `orders` row and stamp `gifts.order_pk` + `line_item_index = 0`.
  Tested against a copy of the production DB via the real `migrate.ts`
  runner — schema bumped to 18, no foreign-key violations.

**Parser — src/lib/server/amazon-parser.ts.**
- New `ParsedAmazonItem = { title, priceCents, quantity }` and
  `parseAmazonEmail().items: ParsedAmazonItem[]`.
- New `extractItemsFromBody()` does a two-pass walk: pass 1 finds
  every `* TITLE / Quantity: N` header and its byte range; pass 2
  pulls the first `$X.XX` inside that range as the line-item price.
  Tolerates items without an inline price (priceCents = null).
  Backed by 4 new tests including a faithful reproducer of order
  #113-2234245-9301002's four line items.

**Persistence — src/lib/server/jobs/amazon-import.ts.**
- `insertRowStmt` now includes `parsed_items_json`.
- New `orders.ts` helper module with `upsertOrderByOrderId()` (fill-
  only via COALESCE; safe to call across order_placed → shipped →
  delivered).
- New `ensureOrderForRow()` upserts on the row's `parsed_order_id`,
  called from `resolveOrCreateGift()` so every Amazon import (single-
  or multi-item) gets a parent order row going forward.
- New `commitMultiItemAccept()` creates 1 order + N gifts when the
  admin specified per-line recipients in the review UI.
- `applyLifecycleEvent()` now walks the `order_pk` sibling set —
  shipped/delivered emails advance ALL gifts under the order, not just
  one. Falls back to single-gift behavior when no order_pk exists
  (legacy gifts).

**Commit input — `CommitRowInput.lineItems`.**
- Optional array of `{lineItemIndex, assignedPersonId, assignedGiftId?}`.
- When present, the loop routes to the multi-item helper and skips the
  single-gift legacy path for that decision.

**Review UI — /admin/imports/amazon/review.**
- Loader parses `parsed_items_json` into `rowItems[r.id]` and also runs
  per-line-item weak match (`lineItemMatches['<rowId>:<idx>']`).
- Pending rows with 2+ line items render a `.line-items` fieldset
  containing one `<li>` per item: title + price + recipient picker
  + per-item weak-match candidates. A "Same recipient for all N"
  shortcut bulk-updates every picker via a small client-side helper.
- For multi-item rows, the legacy single-row gift-link block and
  "Or assign to person" picker are hidden (they'd be misleading —
  each item gets its own).
- `parseDecisions()` gathers `lineperson_<row>_<idx>` and
  `linegift_<row>_<idx>` form fields into the new
  `CommitRowInput.lineItems` array.

**Backfill route — `?/reSplit` on /admin/imports/amazon.**
- New `reImportOrderById()` re-fetches the original Gmail message,
  re-parses with the new multi-item logic, refreshes
  `parsed_items_json` + `parsed_title` + `parsed_price_cents` on the
  existing order_placed import row, archives the collapsed gift with
  an audit-log breadcrumb (restorable from `/app/gifts/[id]`), and
  flips the row's disposition back to `pending` so the admin can
  assign N recipients from the review UI.
- Wired up as a `?/reSplit` form action on the Amazon imports
  landing page with an order-ID input matching the `XXX-XXXXXXX-XXXXXXX`
  pattern. Idempotent — safe to re-run.

### Deferred / follow-up
- **Order-keyed Shippo registration.** The Explore-agent findings
  confirmed Shippo dedupes natively on `(carrier, tracking_number)`,
  so a multi-item order with N gifts sharing a tracking number won't
  double-bill at $0.01/registration today. The cosmetic improvement
  (move `tracking_provider_id` from per-gift to per-order, refactor
  `tracking.ts` to key on `orderPk`) can ride a follow-up commit.
- **Drop legacy `gifts.tracking_*` columns.** Pinned to a one-release
  window for safe rollback; a future migration can drop them once new
  code has soaked.

### Verification
- `npm run check` — 0 errors, 0 warnings.
- `npm test` — 62/62 passing (4 new multi-item parser tests + 6 TZ
  tests from Phase 1).
- `npm run build` — clean.
- Migration applied via real `migrate.ts` runner against a copy of
  the prod DB: schema → 18, foreign_key_check clean.
- Smoke after deploy: paste order #113-2234245-9301002 into the
  Re-split form on /admin/imports/amazon, confirm the review page
  shows 4 line items with 4 recipient pickers, assign per-line,
  commit, confirm 1 orders row + 4 gifts exist with correct
  `order_pk` and `line_item_index` values.

### Files
- `migrations/018-orders-table.sql` (new) — orders table, FK columns,
  parsed_items_json column, backfill.
- `src/lib/server/orders.ts` (new) — upsert/list helpers.
- `src/lib/server/types.ts` — Order + Gift.order_pk/line_item_index.
- `src/lib/server/gifts.ts` — createGift accepts order_pk/line_item_index.
- `src/lib/server/amazon-parser.ts` — ParsedAmazonItem,
  extractItemsFromBody.
- `src/lib/server/amazon-parser.test.ts` — 4 new tests.
- `src/lib/server/jobs/amazon-import.ts` — multi-item commit,
  ensureOrderForRow, order-walk applyLifecycleEvent, reImportOrderById.
- `src/routes/admin/imports/amazon/review/+page.server.ts` — rowItems,
  lineItemMatches, lineItems decision parsing.
- `src/routes/admin/imports/amazon/review/+page.svelte` — per-line
  picker block, apply-all shortcut, .line-items styles.
- `src/routes/admin/imports/amazon/+page.server.ts` — `?/reSplit` action.
- `src/routes/admin/imports/amazon/+page.svelte` — re-split form.

---

## 10:40 EDT — Two post-deploy hotfixes to Today's Next Best Thing

### Summary
After shipping Phase 1 + Phase 2 (commit 803b521), Ambree still appeared
as the Next Best Thing on /app/today with "no gift marked bought yet"
despite having two ordered gifts. Two related bugs in how Today decides
who "still needs attention" — both fixed and deployed in quick
succession (commits 80091c0 and 1862b0a).

### What Was Done

**Hotfix 1 (80091c0) — nextBestAction filter too narrow.**
`loadTodayData` was filtering needsAttention purely on `hasHandledGift`,
which uses the intentionally narrow `{given, returned}` set so Today
keeps people visible all the way through wrap/deliver. But the Next
Best Thing card's copy is literally "no gift marked bought yet" — a
purchase prompt. Someone with an `ordered`/`shipped`/`wrapped` gift
should fall through to Coming Up Soon (where they get the BOUGHT pill)
instead of getting prompted to buy again. Added a `BOUGHT_STATUSES`
set (`ordered, shipped, delivered, wrapped, given, returned`) and
extended the needsAttention filter to also exclude anyone whose
`bestGiftStatus` is in that set. `planned` (= "Chosen, not bought yet")
still flows through as a valid prompt.

**Hotfix 2 (1862b0a) — NULL occasion_id attribution bug.**
After hotfix 1 deployed, Ambree STILL appeared as Next Best Thing.
Root cause: `annotateGiftState` only matches gifts on `person_id =
? AND occasion_id = ?`. Amazon-imported gifts get `occasion_id =
NULL` until the admin explicitly assigns one, so they didn't count
against any of the person's upcoming occasions. Bruce and Carter
must have had `occasion_id` set explicitly on their graduation
gifts, which is why their pills already worked.

Fix: in both `today.ts` and `reminders.ts`, after the exact (person,
occasion) probe misses, fall back to a person-only query that catches
NULL-occasion gifts — but only the **nearest** upcoming occasion claims
them. A single ordered Amazon gift can't legitimately cover a
graduation tomorrow AND a birthday 6 months out, so per-person
"looseClaimed" guard ensures the closest occasion wins, the others
stay unattributed.

### Files Modified
- `src/lib/server/today.ts` — `BOUGHT_STATUSES`, broadened
  needsAttention filter, two-stmt annotateGiftState with per-person
  loose-claim logic.
- `src/lib/server/jobs/reminders.ts` — same two-stmt + loose-claim
  pattern in `collectUpcoming` (so the daily Telegram digest tracks
  the same attribution rules as the UI).

### Key Technical Insight
The Today list has TWO meaningfully different "is this person handled"
predicates living in one place:

1. **HANDLED_STATUSES `{given, returned}`** — keeps the *Coming Up
   Soon* list populated through wrap/deliver so the manager can see
   gifts on the shelf until they're physically given. This is the
   right semantics for "should this row stay visible at all?".
2. **BOUGHT_STATUSES `{ordered..returned}`** — gates the *Next Best
   Thing* prompt. The card says "no gift marked bought yet"; that's
   a purchase-completion question, not a give-completion question.
   Bought-but-not-given still needs attention conceptually, but the
   right surface is Coming Up Soon, not a "buy something!" hero.

Conflating these two collapsed into the same `hasHandledGift` boolean
caused the bug. The fix splits them: `hasHandledGift` stays narrow for
visibility, `bestGiftStatus` carries the granular state, and the
nextBestAction filter combines both.

Second insight: Amazon imports landing with `occasion_id=NULL` is a
real architectural sharp edge. The pragmatic fix (loose match claimed
by nearest occasion) works in the common case but isn't fundamental.
A cleaner long-term answer is to prompt for occasion assignment at
import-review time, alongside the per-line recipient pickers that
Phase 2 just added.

### Next Steps
- Watch tomorrow's reminder digest to confirm the loose-claim logic
  works there too — the cron fires at 8:00 ET and Ambree's grad is
  on May 15.
- Backfill the collapsed 4-item Amazon order #113-2234245-9301002
  using the new `?/reSplit` button on /admin/imports/amazon.
- Consider: prompt for occasion assignment in the Amazon review UI
  so newly imported gifts arrive with `occasion_id` already set
  instead of leaning on the loose-match fallback.
- Re-enable the deferred Phase 2 polish items eventually:
  order-keyed Shippo registration and dropping the legacy
  `gifts.tracking_*` columns.

---

## 13:15 EDT — Tracking import: orphan-self-package bug (td-3d1ee6)

### Summary
A Sunmed CBD shipment-notification for order #241943 created a new
orphan self-package ("A shipment from order #241943 is on the way")
instead of attaching the USPS tracking number to the existing order-
confirmation gift on the same order. Fixed the silent match miss and
deployed (commit ccdd97f).

### What Was Done
- Added a third clause to `acceptTrackingRow`'s order-id match query:
  `p.is_self = 1 AND p.owner_user_id = ?`. The cross-customer order#
  collision the existing vendor/source_url constraint was guarding
  against can't happen inside one actor's own self-packages, so this
  is a safe extension.
- `acceptOrderConfirmationRow` now stamps `source_url` with
  `https://{senderDomain}/` on create. Future order_confirmation
  self-packages will match via the pre-existing INSTR clause without
  needing the new clause (c) fallback. Legacy gifts already missing
  source_url are covered by (c).
- `acceptTrackingRow`'s step-3 fallback (create new self-package) now
  logs a structured warn with order_id + sender + tracking so silent
  orphan creations show up in pm2 logs.

### Files Modified
- `src/lib/server/jobs/tracking-import.ts` — three changes above.

### Key Technical Insight
The original constraint — "order# match must agree with vendor OR
source_url" — was the right design choice to prevent two different
customers' "#12345" orders at different merchants from cross-matching.
But it implicitly assumed all gifts had a meaningful vendor link or
source_url. The `acceptOrderConfirmationRow` create path violated that
assumption: it generated bare self-packages with vendor_id=NULL and
source_url=NULL, leaving no anchor for the constraint to grip.

Lesson: when adding a new "create gift" path, audit every existing
"match gift" path's filter to make sure newly-created shapes can be
re-found. The order_confirmation feature (td-c28c5e) and the order-id
match guard (predating td-3d1ee6) were written separately and the
constraint mismatch wasn't surfaced until a real shipment notification
landed weeks later.

The td description's hypotheses (type coercion, case, whitespace) were
all reasonable; the actual cause was structural and one layer up from
the actual order_id comparison.

### Next Steps
- Re-scan the tracking inbox (the user manually moved the Sunmed
  shipment email back). To exercise the new clause (c), archive the
  prior orphan first so step 1 (tracking-number match) doesn't claim
  it. Confirm the original order_confirmation gift now ends up with
  the tracking number attached and registered with Shippo.
- Possible follow-up: a review queue for unmatched tracking emails
  instead of silently creating self-packages. The td description
  flagged this as a separate concern; the new console.warn is a
  minimum-effort first step toward visibility.
