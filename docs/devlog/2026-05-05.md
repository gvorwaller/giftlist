# 2026-05-05 Devlog

## 14:10 EDT - Filter self-orders out of gift-manager views (td-24eac3)

### Summary
Implemented the scope-expansion filter so personal (non-gift) orders can ride
the same database without polluting the gift-manager flows. `people.is_self`
flag added; `/app/today`, `/app/people`, and reminder digests now hide self-
people while `/app/packages` continues to show everything in flight.

### What Was Done
- **Schema (migration 012-people-is-self.sql)**: added `is_self INTEGER NOT NULL DEFAULT 0 CHECK (is_self IN (0, 1))` to `people` plus an index. All existing rows default to `is_self=0`. Verified by applying to a copy of the dev DB and inspecting the schema.
- **Type + data layer (`src/lib/server/types.ts`, `people.ts`)**:
  - `Person.is_self: 0 | 1` field added.
  - `PersonUpsertInput.is_self?: boolean` so admin forms can write the flag.
  - `listPeople({ includeSelf })` defaults to `false` (manager views) and `true` only where the admin opts in. Implemented by appending `p.is_self = 0` to the WHERE clause when `includeSelf` is omitted.
  - `createPerson` / `updatePerson` honor the flag; updatePerson preserves the prior value when the input field is undefined (so non-admin code paths can't accidentally clear the flag).
- **Manager-view filters**:
  - `today.ts loadUpcomingWindow()` JOIN now requires `p.is_self = 0`.
  - `today.ts` packagesOnTheWay query joins `people` and excludes self.
  - `jobs/reminders.ts collectUpcoming()` and `collectPackages()` both gain the same filter — keeps the digest focused on actual gifts.
  - `admin-home.ts countIncompletePeople()` / `listIncompletePeople()` also exclude self so the admin "incomplete people" warning doesn't fire just because Gaylon's self-row has no birthday.
- **Admin UI**:
  - `/admin/people` listing now passes `includeSelf: true`, shows a "Self" badge + "(me)" inline tag + amber subline ("Personal orders — hidden from manager views").
  - `/admin/people/new` and `/admin/people/[id]` got an `is_self` checkbox with descriptive helper text.
- **Manager UI**:
  - `/app/people` keeps the default filter, so self-people don't appear there.
  - `/app/gifts/new` includes self-people in the dropdown with a `(me)` suffix; the load function skips occasion auto-pick when the active person is self (occasion stays optional, no auto-default to nearest birthday).
- **Verification**: `npm run check` clean (0 errors / 0 warnings). Boot smoke test confirmed migration 012 applied successfully on the dev DB (`schema_version=12`, all 49 people defaulted to `is_self=0`).

### Files Modified
- `migrations/012-people-is-self.sql` (new)
- `src/lib/server/types.ts`
- `src/lib/server/people.ts`
- `src/lib/server/today.ts`
- `src/lib/server/jobs/reminders.ts`
- `src/lib/server/admin-home.ts`
- `src/routes/admin/people/+page.server.ts`
- `src/routes/admin/people/+page.svelte`
- `src/routes/admin/people/new/+page.server.ts`
- `src/routes/admin/people/new/+page.svelte`
- `src/routes/admin/people/[id]/+page.server.ts`
- `src/routes/admin/people/[id]/+page.svelte`
- `src/routes/app/gifts/new/+page.server.ts`
- `src/routes/app/gifts/new/+page.svelte`

### Key Technical Insight
Chose option (a) `is_self` boolean over option (b) reserved-row pattern. Three reasons: (1) keeps the person model uniform — self-orders still have a real person row, just with a flag; (2) the filter generalizes (`WHERE is_self = 0`) instead of relying on a magic ID lookup that would need to be threaded through every query; (3) supports multiple selves later without code changes (e.g., spouse's orders later become a second self-row).

The "preserve prior value when input.is_self is undefined" detail in `updatePerson` is intentional — non-admin code paths (e.g., a future contacts-import refresh) shouldn't accidentally toggle the flag just because they don't pass it. Admin forms always pass it (checkbox state).

### Next Steps
1. Manual verification on dev: create a self-person via `/admin/people/new`, add a personal order via `/app/gifts/new`, confirm it shows on `/app/packages` and is absent from `/app/today` + `/app/people` + reminder digest preview.
2. Once confirmed, move td-24eac3 from `in_review` to `closed` and deploy.
3. Real-world smoke test still pending for td-81455a (Shippo end-to-end on a real shipment).

## 14:28 EDT - Codex review patches (td-24eac3)

### Summary
Codex review of the uncommitted td-24eac3 diff caught three real leak/foot-gun
paths that the AC didn't enumerate. Patched all three.

### Findings + Fixes
1. **Self-person leak through `recently_viewed` + `/app/people/[id]`** — the manager-facing person detail route only blocked archived rows, so a self-person was reachable via direct URL, which then wrote to `recently_viewed`, which `/app/today` rendered without filtering.
   - `src/routes/app/people/[id]/+page.server.ts`: now also 404s when `person.is_self === 1`.
   - `src/lib/server/today.ts`: replaced the `listRecent` call with an inline query that filters out self-person rows AND gift rows whose person is_self. Also dropped the now-unused `listRecent` import.
2. **Editing a self-order from `/app/packages` was broken** — `/app/packages` (correctly) shows self-orders, but `/app/gifts/[id]/edit` built its people dropdown from `listPeople` defaults (excluded self), so the form's required `<select>` had no option matching the gift's person_id and the page failed validation.
   - `src/routes/app/gifts/[id]/edit/+page.server.ts`: pass `includeSelf: true` to listPeople; if the gift's person is_self, return an empty `personOccasions` list.
   - `src/routes/app/gifts/[id]/edit/+page.svelte`: append " (me)" suffix to self options, matching the gift-new form.
   - Bonus: `src/routes/app/gifts/[id]/+page.svelte`: don't render person-detail links for self-orders (they would 404 now); render plain text instead.
3. **Google Contacts import could silently adopt a self-row by full_name** — the preview + commit paths matched existing people via `full_name` only, so a self-person whose full_name happened to match a Google contact would be adopted and have a birthday assigned to it.
   - `src/lib/server/contacts-import.ts`: both `byFullName` queries gain `AND is_self = 0`.

### Codex non-issues (cleared)
- `updatePerson` "preserve prior is_self when undefined" logic — verified not bypassed; contacts-import uses direct SQL for adoption/insert paths, never `updatePerson`.
- `/admin/audit` filtering — audit listing reads from `audit_log` directly with no `people` join, so it's not affected.
- Migration interaction with the partial unique index on `google_resource_name` — additive only, no conflict.

### Files Modified
- `src/routes/app/people/[id]/+page.server.ts`
- `src/lib/server/today.ts`
- `src/routes/app/gifts/[id]/+page.svelte`
- `src/routes/app/gifts/[id]/edit/+page.server.ts`
- `src/routes/app/gifts/[id]/edit/+page.svelte`
- `src/lib/server/contacts-import.ts`

### Verification
- `npm run check` clean (0 errors / 0 warnings) after all patches.

### Key Technical Insight
The `recently_viewed` leak path was the most subtle one — even with `/app/today`, `/app/people`, and the reminder digest all filtered correctly, a self-person could still appear on the Today screen because the recent-history rendering trusted the table at face value. Lesson: any user-facing list that joins to `people` (even transitively, via a foreign key like `recently_viewed.entity_id`) needs the same `is_self = 0` filter applied at render time, not just at write time. The "filter at read, defense in depth" approach handles the case where rows were written before is_self was introduced or before the filter was added.

## 14:50 EDT - "Add a package" entry point + self-order UX polish (td-24eac3)

### Summary
After deploying td-24eac3 the user couldn't find any way to add a personal package — no Add button on `/app/packages`, and the gift form's "planned" default status meant self-orders never landed on the packages list without a manual transition. Fixed both gaps.

### What Was Done
- **`/app/packages` Add button**: replaced the lonely "Refresh all tracking" with an actions row that puts a primary "Add a package" link first. Empty-state copy now also points at it.
- **Self-aware gift form**:
  - When the picked person is_self, the form hides "Just an idea for now" + the occasion field (no birthday/holiday context for self-orders)
  - Auto-defaults status to `ordered` so the gift lands on `/app/packages` immediately
  - Auto-opens "More details" so tracking# / shipper are one tap away
  - Shows an amber note: "Personal package — saves as 'ordered' so it shows on Packages"
- **Server**: `/app/gifts/new` POST now accepts `status='ordered'` (previously only idea/planned were allowed). `createGift` doesn't accept `ordered_at`, but the packages list `COALESCE(shipped_at, ordered_at, created_at)` falls through to created_at so sorting still works.

### Files Modified
- `src/routes/app/packages/+page.svelte`
- `src/routes/app/gifts/new/+page.svelte`
- `src/routes/app/gifts/new/+page.server.ts`

### Key Technical Insight
The original AC for td-24eac3 specified "self-orders show on /app/packages" but said nothing about *getting them there*. The implementation passed all the criteria but had no entry point — a real "the spec is technically correct but the feature is unusable" gap. Lesson: when delivering a feature, walk the user's actual end-to-end flow, not just the AC checklist.

Deployed: `2a3452b`.

## 14:55 EDT - Shippo error surfacing + CF 502 fix

### Summary
First real-world test of self-order tracking returned a 502 Bad Gateway from Cloudflare instead of an inline error. Root cause: Shippo refused the API call with HTTP 401 ("Your account needs to have a valid payment method on file"), our `fail(502, ...)` from the refresh action got intercepted by CF's branded error page, and the user couldn't see what was wrong. Patched two ways.

### What Was Done
- **Status code change**: `/app/gifts/[id]` and `/app/packages` refresh actions now `fail(400)` instead of `fail(502)/fail(503)` for upstream Shippo errors. Cloudflare passes 4xx through; 5xx triggers CF's "Bad gateway" branded page that swallows the response body. Same semantically-meaningful error, but the user actually sees it.
- **`shippoFetch` error parsing**: extracts `detail` from Shippo's JSON envelope (`{"detail": "Your account needs..."}`) and surfaces it directly in the thrown Error message. User now sees `"Shippo (HTTP 401): Your account needs a valid payment method..."` instead of the raw HTTP/JSON dump.
- **User-side fix**: added a payment method to Shippo (https://goshippo.com/user/billing/). Tracking API is pay-as-you-go ($0.01/registration, well under $1/yr at this volume) but Shippo gates the live key behind billing setup even at $0 spend.

After billing was added, the UPS tracking number registered cleanly, returned `PRE_TRANSIT` with ETA Wed May 6, and the journey checkpoint rendered as expected.

### Files Modified
- `src/routes/app/gifts/[id]/+page.server.ts`
- `src/routes/app/packages/+page.server.ts`
- `src/lib/server/tracking.ts`

### Key Technical Insight
Cloudflare's "intercept 5xx and serve branded page" behavior is on by default for our setup, and silently undermines SvelteKit's `fail()` pattern when the failure is actually upstream. Use 4xx for any business-logic / upstream-rejection failure that you want users to see; reserve 5xx for genuine app-layer crashes. (Or whitelist origin error pages in CF — but that's per-domain config and easy to forget.)

Deployed: `a78393f`.

## 15:30 EDT - Per-user privacy on self-orders (td-68804e)

### Summary
Manager Madonna logged in and saw admin Gaylon's "Circupool chlorine generator" personal order on `/app/packages`, complete with title, vendor, price, and tracking#. Real P1 privacy bug — the `is_self` filter was binary (hide all from manager views, show all on packages) with no per-user scoping. Added an `owner_user_id` to people so each user sees only their own self-orders. Codex review caught five additional bugs before commit.

### What Was Done
- **Migration 013** (`013-people-owner-user.sql`): added `people.owner_user_id INTEGER REFERENCES users(id) ON DELETE RESTRICT` plus index. Backfilled all existing `is_self = 1` rows to the first admin's user_id (Gaylon's existing self-row got owner_user_id=1).
- **Data layer**:
  - `Person.owner_user_id` field
  - `listPeople({ selfOwnerUserId })` filter — when set with `includeSelf:true`, only returns self-people whose owner matches (strict equality, no null-allowance)
  - `createPerson` defaults owner to actorUserId when is_self is set; `updatePerson` preserves owner when input is undefined and clears it when is_self goes 1→0
  - New `isPersonVisibleToUser(personId, userId)` server-side guard
  - `pullAllInFlight(actorUserId, viewerUserId?)` got an optional viewer-scoping arg
- **Privacy enforcement**:
  - `/app/packages` query filters `WHERE p.is_self = 0 OR p.owner_user_id = ?`
  - `/app/packages` "Refresh all tracking" action passes the user as viewer to scope counts (P0 fix from Codex review — was previously global)
  - `/app/gifts/[id]` + `/edit` 404 when `gift.person.is_self === 1 && person.owner_user_id !== currentUserId` (closes direct-URL leak)
  - `/app/gifts/new` + `/edit` POSTs call `isPersonVisibleToUser` server-side (P1 — dropdown was client-only filtered, crafted POST could've reassigned a gift to admin's self)
  - `today.ts` recently_viewed filter extended to "self that's not mine"
- **Admin UX**:
  - `/admin/people/new` and `/[id]` got an Owner `<select>` (visible only when is_self is checked, populated from `listUsers()`, defaults to current actor)
  - `/admin/people` listing surfaces "Personal tracker · owner: Gaylon" so admin can tell whose tracker is whose
  - `/admin/people/[id]` action now returns `submittedValues` on `fail()` and svelte uses `untrack()` to seed inputs from `form.values` first, then stored row (P1 — was previously snapping back to stored values on validation failure)
- **`auth.ts`**: new `listUsers()` helper for the owner picker

### Files Modified
- `migrations/013-people-owner-user.sql` (new)
- `src/lib/server/types.ts`, `people.ts`, `today.ts`, `tracking.ts`, `auth.ts`
- `src/routes/admin/people/+page.server.ts` + `+page.svelte`
- `src/routes/admin/people/new/+page.server.ts` + `+page.svelte`
- `src/routes/admin/people/[id]/+page.server.ts` + `+page.svelte`
- `src/routes/app/gifts/[id]/+page.server.ts`
- `src/routes/app/gifts/[id]/edit/+page.server.ts`
- `src/routes/app/gifts/new/+page.server.ts`
- `src/routes/app/packages/+page.server.ts`

### Codex review findings (all patched in same commit)
| Pri | Bug | Fix |
|---|---|---|
| P0 | `/app/packages` "Refresh all tracking" was global → leaked self-order counts to manager and triggered Shippo refreshes on hidden gifts | Added `viewerUserId` param to `pullAllInFlight` |
| P1 | Gift create/edit POSTs only validated `getPersonById` — crafted POST could reassign a gift to admin's self-person | New `isPersonVisibleToUser` server guard, called from both POST actions and the `?person=` prefill path |
| P1 | Admin edit form let `is_self=true` with empty owner persist `NULL` | Falls back to prior owner, then to actor; never saves null |
| P1 | Admin edit form didn't preserve typed values on `fail()` | Action returns `values`; svelte reads `form.values` via untrack snapshot |
| P2 | `OR p.owner_user_id IS NULL` allowance + `ON DELETE SET NULL` could manufacture orphan self-rows that leaked | Strict equality everywhere; FK changed to `ON DELETE RESTRICT` so user deletion fails loudly until self-people are reassigned |

### Verification
- Admin sees Circupool order on `/app/packages` ✓
- Madonna's `/app/packages` is empty (no Circupool) ✓
- Madonna's direct URL to gift detail returns 404 ✓
- Owner field on `/admin/people/Gaylon Me` shows "Gaylon — me" ✓
- Schema v13 live in production

### Key Technical Insight
The original td-24eac3 design treated self-orders as a single boolean (visible-to-everyone-on-packages, hidden-from-everyone-elsewhere). This worked for Gaylon's "one-stop-shop tracking" use case but missed the multi-user dimension entirely — which only became visible the moment a real second user actually logged in.

The owner_user_id model is the right shape: regular gift recipients are global (no owner), self-people are private (owner = a specific user), and the filter generalizes across `/app/packages`, gift detail/edit, recently_viewed, and the gift-form dropdown using the same predicate. Strict equality (no null-owner allowance) keeps the model fail-closed: an orphaned self-row is invisible to everyone, not silently visible to the surviving user.

Codex's findings reinforced the importance of treating client-side filtering as defense-in-depth only — the `selfOwnerUserId` filter on the gift-form dropdown made the UI *appear* private, but a crafted POST from a manager could still target admin's self-person ID until the matching server-side guard was added.

Deployed: `72c6b93`.

### Next Steps
- td-24eac3 closed
- td-68804e closed
- td-1b8ded ("Surface remaining .env knobs in /admin/settings UI") still open at P1, deferred per user's call
- td-81455a (Shippo end-to-end smoke test) effectively validated by today's UPS shipment — webhook will confirm DELIVERED state when carrier scans complete
