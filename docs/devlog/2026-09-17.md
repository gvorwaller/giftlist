# 2026-09-17 Devlog

## td-665d01 — release and production schema recovery

User authorized deployment of the shipment reconciliation change. Re-ran all 153 tests, type checks and production build, then committed/pushed 8d0d855 and deployed through `scripts/deploy-to-DO.sh` to both configured instances with database snapshots.

Post-deploy verification found that the main instance's **pre-deploy backup** already recorded `schema_version=29`, but had none of migration 029's new objects (the order-reference column and two item-resolution tables). Thus the version-only migration gate skipped the migration. The second instance started at 28 and upgraded normally. A generic health check initially passed despite the main schema mismatch; the explicit table/route smoke check caught it. The origin of the pre-existing marker is not established.

Added recovery for this exact all-absent state: apply migration 029 transactionally without lowering a newer version marker. Partial schemas fail explicitly rather than being silently accepted. `/healthz` now verifies the shipment schema objects and returns 503 if incomplete. Three regression tests cover recovery, preservation of existing gifts/idempotence, partial-state refusal and preservation of newer markers.

### Existing emails

The AI re-run action refreshes purchase-matcher verdicts, not email parsing/type classification. Pending shipment/delivery rows use the new planner on review-page load, but older rows retain only their previously parsed order reference; manual existing-gift selection remains available. Accepted imports are not replayed. No historical gifts or Gmail labels are repaired by deployment.

Read-only production inspection: September 17 Amazon run 504 has six unknown pending messages (three promotions and three return confirmations), plus one skipped review request. September 16 run 502 has a pending shipment and delivery notification. Neither scan nor AI re-run reparses already-staged messages.

Final deployment verification is recorded in td-665d01's release log, including exact revision, health, schema objects, review-page response and comparison of gifts against the pre-deploy snapshots.
